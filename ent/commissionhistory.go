// Code generated by ent, DO NOT EDIT.

package ent

import (
	"affluo/ent/commissionhistory"
	"affluo/ent/user"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CommissionHistory is the model entity for the CommissionHistory schema.
type CommissionHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount float64 `json:"amount,omitempty"`
	// AffiliateUserID holds the value of the "affiliate_user_id" field.
	AffiliateUserID string `json:"affiliate_user_id,omitempty"`
	// TrxID holds the value of the "trx_id" field.
	TrxID string `json:"trx_id,omitempty"`
	// TrackID holds the value of the "track_id" field.
	TrackID string `json:"track_id,omitempty"`
	// CommissionRate holds the value of the "commission_rate" field.
	CommissionRate float64 `json:"commission_rate,omitempty"`
	// IsFirstOrder holds the value of the "is_first_order" field.
	IsFirstOrder bool `json:"is_first_order,omitempty"`
	// Date holds the value of the "date" field.
	Date string `json:"date,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CommissionHistoryQuery when eager-loading is set.
	Edges                     CommissionHistoryEdges `json:"edges"`
	user_commission_histories *int64
	selectValues              sql.SelectValues
}

// CommissionHistoryEdges holds the relations/edges for other nodes in the graph.
type CommissionHistoryEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CommissionHistoryEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CommissionHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case commissionhistory.FieldIsFirstOrder:
			values[i] = new(sql.NullBool)
		case commissionhistory.FieldAmount, commissionhistory.FieldCommissionRate:
			values[i] = new(sql.NullFloat64)
		case commissionhistory.FieldID:
			values[i] = new(sql.NullInt64)
		case commissionhistory.FieldAffiliateUserID, commissionhistory.FieldTrxID, commissionhistory.FieldTrackID, commissionhistory.FieldDate:
			values[i] = new(sql.NullString)
		case commissionhistory.ForeignKeys[0]: // user_commission_histories
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CommissionHistory fields.
func (ch *CommissionHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case commissionhistory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ch.ID = int64(value.Int64)
		case commissionhistory.FieldAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value.Valid {
				ch.Amount = value.Float64
			}
		case commissionhistory.FieldAffiliateUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field affiliate_user_id", values[i])
			} else if value.Valid {
				ch.AffiliateUserID = value.String
			}
		case commissionhistory.FieldTrxID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field trx_id", values[i])
			} else if value.Valid {
				ch.TrxID = value.String
			}
		case commissionhistory.FieldTrackID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field track_id", values[i])
			} else if value.Valid {
				ch.TrackID = value.String
			}
		case commissionhistory.FieldCommissionRate:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field commission_rate", values[i])
			} else if value.Valid {
				ch.CommissionRate = value.Float64
			}
		case commissionhistory.FieldIsFirstOrder:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_first_order", values[i])
			} else if value.Valid {
				ch.IsFirstOrder = value.Bool
			}
		case commissionhistory.FieldDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				ch.Date = value.String
			}
		case commissionhistory.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_commission_histories", value)
			} else if value.Valid {
				ch.user_commission_histories = new(int64)
				*ch.user_commission_histories = int64(value.Int64)
			}
		default:
			ch.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CommissionHistory.
// This includes values selected through modifiers, order, etc.
func (ch *CommissionHistory) Value(name string) (ent.Value, error) {
	return ch.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the CommissionHistory entity.
func (ch *CommissionHistory) QueryUser() *UserQuery {
	return NewCommissionHistoryClient(ch.config).QueryUser(ch)
}

// Update returns a builder for updating this CommissionHistory.
// Note that you need to call CommissionHistory.Unwrap() before calling this method if this CommissionHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (ch *CommissionHistory) Update() *CommissionHistoryUpdateOne {
	return NewCommissionHistoryClient(ch.config).UpdateOne(ch)
}

// Unwrap unwraps the CommissionHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ch *CommissionHistory) Unwrap() *CommissionHistory {
	_tx, ok := ch.config.driver.(*txDriver)
	if !ok {
		panic("ent: CommissionHistory is not a transactional entity")
	}
	ch.config.driver = _tx.drv
	return ch
}

// String implements the fmt.Stringer.
func (ch *CommissionHistory) String() string {
	var builder strings.Builder
	builder.WriteString("CommissionHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ch.ID))
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", ch.Amount))
	builder.WriteString(", ")
	builder.WriteString("affiliate_user_id=")
	builder.WriteString(ch.AffiliateUserID)
	builder.WriteString(", ")
	builder.WriteString("trx_id=")
	builder.WriteString(ch.TrxID)
	builder.WriteString(", ")
	builder.WriteString("track_id=")
	builder.WriteString(ch.TrackID)
	builder.WriteString(", ")
	builder.WriteString("commission_rate=")
	builder.WriteString(fmt.Sprintf("%v", ch.CommissionRate))
	builder.WriteString(", ")
	builder.WriteString("is_first_order=")
	builder.WriteString(fmt.Sprintf("%v", ch.IsFirstOrder))
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(ch.Date)
	builder.WriteByte(')')
	return builder.String()
}

// CommissionHistories is a parsable slice of CommissionHistory.
type CommissionHistories []*CommissionHistory
