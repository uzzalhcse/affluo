// Code generated by ent, DO NOT EDIT.

package ent

import (
	"affluo/ent/banner"
	"affluo/ent/bannercreative"
	"affluo/ent/creative"
	"affluo/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CreativeQuery is the builder for querying Creative entities.
type CreativeQuery struct {
	config
	ctx                 *QueryContext
	order               []creative.OrderOption
	inters              []Interceptor
	predicates          []predicate.Creative
	withBanners         *BannerQuery
	withBannerCreatives *BannerCreativeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CreativeQuery builder.
func (cq *CreativeQuery) Where(ps ...predicate.Creative) *CreativeQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CreativeQuery) Limit(limit int) *CreativeQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CreativeQuery) Offset(offset int) *CreativeQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CreativeQuery) Unique(unique bool) *CreativeQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CreativeQuery) Order(o ...creative.OrderOption) *CreativeQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryBanners chains the current query on the "banners" edge.
func (cq *CreativeQuery) QueryBanners() *BannerQuery {
	query := (&BannerClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(creative.Table, creative.FieldID, selector),
			sqlgraph.To(banner.Table, banner.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, creative.BannersTable, creative.BannersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBannerCreatives chains the current query on the "banner_creatives" edge.
func (cq *CreativeQuery) QueryBannerCreatives() *BannerCreativeQuery {
	query := (&BannerCreativeClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(creative.Table, creative.FieldID, selector),
			sqlgraph.To(bannercreative.Table, bannercreative.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, creative.BannerCreativesTable, creative.BannerCreativesColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Creative entity from the query.
// Returns a *NotFoundError when no Creative was found.
func (cq *CreativeQuery) First(ctx context.Context) (*Creative, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{creative.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CreativeQuery) FirstX(ctx context.Context) *Creative {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Creative ID from the query.
// Returns a *NotFoundError when no Creative ID was found.
func (cq *CreativeQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{creative.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CreativeQuery) FirstIDX(ctx context.Context) int64 {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Creative entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Creative entity is found.
// Returns a *NotFoundError when no Creative entities are found.
func (cq *CreativeQuery) Only(ctx context.Context) (*Creative, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{creative.Label}
	default:
		return nil, &NotSingularError{creative.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CreativeQuery) OnlyX(ctx context.Context) *Creative {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Creative ID in the query.
// Returns a *NotSingularError when more than one Creative ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CreativeQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{creative.Label}
	default:
		err = &NotSingularError{creative.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CreativeQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Creatives.
func (cq *CreativeQuery) All(ctx context.Context) ([]*Creative, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryAll)
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Creative, *CreativeQuery]()
	return withInterceptors[[]*Creative](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CreativeQuery) AllX(ctx context.Context) []*Creative {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Creative IDs.
func (cq *CreativeQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryIDs)
	if err = cq.Select(creative.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CreativeQuery) IDsX(ctx context.Context) []int64 {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CreativeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryCount)
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CreativeQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CreativeQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CreativeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryExist)
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CreativeQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CreativeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CreativeQuery) Clone() *CreativeQuery {
	if cq == nil {
		return nil
	}
	return &CreativeQuery{
		config:              cq.config,
		ctx:                 cq.ctx.Clone(),
		order:               append([]creative.OrderOption{}, cq.order...),
		inters:              append([]Interceptor{}, cq.inters...),
		predicates:          append([]predicate.Creative{}, cq.predicates...),
		withBanners:         cq.withBanners.Clone(),
		withBannerCreatives: cq.withBannerCreatives.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithBanners tells the query-builder to eager-load the nodes that are connected to
// the "banners" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CreativeQuery) WithBanners(opts ...func(*BannerQuery)) *CreativeQuery {
	query := (&BannerClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withBanners = query
	return cq
}

// WithBannerCreatives tells the query-builder to eager-load the nodes that are connected to
// the "banner_creatives" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CreativeQuery) WithBannerCreatives(opts ...func(*BannerCreativeQuery)) *CreativeQuery {
	query := (&BannerCreativeClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withBannerCreatives = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Creative.Query().
//		GroupBy(creative.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CreativeQuery) GroupBy(field string, fields ...string) *CreativeGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CreativeGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = creative.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Creative.Query().
//		Select(creative.FieldName).
//		Scan(ctx, &v)
func (cq *CreativeQuery) Select(fields ...string) *CreativeSelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CreativeSelect{CreativeQuery: cq}
	sbuild.label = creative.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CreativeSelect configured with the given aggregations.
func (cq *CreativeQuery) Aggregate(fns ...AggregateFunc) *CreativeSelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CreativeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !creative.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	return nil
}

func (cq *CreativeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Creative, error) {
	var (
		nodes       = []*Creative{}
		_spec       = cq.querySpec()
		loadedTypes = [2]bool{
			cq.withBanners != nil,
			cq.withBannerCreatives != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Creative).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Creative{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withBanners; query != nil {
		if err := cq.loadBanners(ctx, query, nodes,
			func(n *Creative) { n.Edges.Banners = []*Banner{} },
			func(n *Creative, e *Banner) { n.Edges.Banners = append(n.Edges.Banners, e) }); err != nil {
			return nil, err
		}
	}
	if query := cq.withBannerCreatives; query != nil {
		if err := cq.loadBannerCreatives(ctx, query, nodes,
			func(n *Creative) { n.Edges.BannerCreatives = []*BannerCreative{} },
			func(n *Creative, e *BannerCreative) { n.Edges.BannerCreatives = append(n.Edges.BannerCreatives, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CreativeQuery) loadBanners(ctx context.Context, query *BannerQuery, nodes []*Creative, init func(*Creative), assign func(*Creative, *Banner)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int64]*Creative)
	nids := make(map[int64]map[*Creative]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(creative.BannersTable)
		s.Join(joinT).On(s.C(banner.FieldID), joinT.C(creative.BannersPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(creative.BannersPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(creative.BannersPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := values[0].(*sql.NullInt64).Int64
				inValue := values[1].(*sql.NullInt64).Int64
				if nids[inValue] == nil {
					nids[inValue] = map[*Creative]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Banner](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "banners" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (cq *CreativeQuery) loadBannerCreatives(ctx context.Context, query *BannerCreativeQuery, nodes []*Creative, init func(*Creative), assign func(*Creative, *BannerCreative)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*Creative)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bannercreative.FieldCreativeID)
	}
	query.Where(predicate.BannerCreative(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(creative.BannerCreativesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CreativeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "creative_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cq *CreativeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CreativeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(creative.Table, creative.Columns, sqlgraph.NewFieldSpec(creative.FieldID, field.TypeInt64))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, creative.FieldID)
		for i := range fields {
			if fields[i] != creative.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CreativeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(creative.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = creative.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CreativeGroupBy is the group-by builder for Creative entities.
type CreativeGroupBy struct {
	selector
	build *CreativeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CreativeGroupBy) Aggregate(fns ...AggregateFunc) *CreativeGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CreativeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, ent.OpQueryGroupBy)
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CreativeQuery, *CreativeGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CreativeGroupBy) sqlScan(ctx context.Context, root *CreativeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CreativeSelect is the builder for selecting fields of Creative entities.
type CreativeSelect struct {
	*CreativeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CreativeSelect) Aggregate(fns ...AggregateFunc) *CreativeSelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CreativeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, ent.OpQuerySelect)
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CreativeQuery, *CreativeSelect](ctx, cs.CreativeQuery, cs, cs.inters, v)
}

func (cs *CreativeSelect) sqlScan(ctx context.Context, root *CreativeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
