// Code generated by ent, DO NOT EDIT.

package ent

import (
	"affluo/ent/campaign"
	"affluo/ent/campaignlink"
	"affluo/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CampaignLinkQuery is the builder for querying CampaignLink entities.
type CampaignLinkQuery struct {
	config
	ctx          *QueryContext
	order        []campaignlink.OrderOption
	inters       []Interceptor
	predicates   []predicate.CampaignLink
	withCampaign *CampaignQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CampaignLinkQuery builder.
func (clq *CampaignLinkQuery) Where(ps ...predicate.CampaignLink) *CampaignLinkQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit the number of records to be returned by this query.
func (clq *CampaignLinkQuery) Limit(limit int) *CampaignLinkQuery {
	clq.ctx.Limit = &limit
	return clq
}

// Offset to start from.
func (clq *CampaignLinkQuery) Offset(offset int) *CampaignLinkQuery {
	clq.ctx.Offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CampaignLinkQuery) Unique(unique bool) *CampaignLinkQuery {
	clq.ctx.Unique = &unique
	return clq
}

// Order specifies how the records should be ordered.
func (clq *CampaignLinkQuery) Order(o ...campaignlink.OrderOption) *CampaignLinkQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// QueryCampaign chains the current query on the "campaign" edge.
func (clq *CampaignLinkQuery) QueryCampaign() *CampaignQuery {
	query := (&CampaignClient{config: clq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := clq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(campaignlink.Table, campaignlink.FieldID, selector),
			sqlgraph.To(campaign.Table, campaign.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, campaignlink.CampaignTable, campaignlink.CampaignColumn),
		)
		fromU = sqlgraph.SetNeighbors(clq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CampaignLink entity from the query.
// Returns a *NotFoundError when no CampaignLink was found.
func (clq *CampaignLinkQuery) First(ctx context.Context) (*CampaignLink, error) {
	nodes, err := clq.Limit(1).All(setContextOp(ctx, clq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{campaignlink.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CampaignLinkQuery) FirstX(ctx context.Context) *CampaignLink {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CampaignLink ID from the query.
// Returns a *NotFoundError when no CampaignLink ID was found.
func (clq *CampaignLinkQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = clq.Limit(1).IDs(setContextOp(ctx, clq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{campaignlink.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CampaignLinkQuery) FirstIDX(ctx context.Context) int64 {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CampaignLink entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CampaignLink entity is found.
// Returns a *NotFoundError when no CampaignLink entities are found.
func (clq *CampaignLinkQuery) Only(ctx context.Context) (*CampaignLink, error) {
	nodes, err := clq.Limit(2).All(setContextOp(ctx, clq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{campaignlink.Label}
	default:
		return nil, &NotSingularError{campaignlink.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CampaignLinkQuery) OnlyX(ctx context.Context) *CampaignLink {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CampaignLink ID in the query.
// Returns a *NotSingularError when more than one CampaignLink ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CampaignLinkQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = clq.Limit(2).IDs(setContextOp(ctx, clq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{campaignlink.Label}
	default:
		err = &NotSingularError{campaignlink.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CampaignLinkQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CampaignLinks.
func (clq *CampaignLinkQuery) All(ctx context.Context) ([]*CampaignLink, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryAll)
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CampaignLink, *CampaignLinkQuery]()
	return withInterceptors[[]*CampaignLink](ctx, clq, qr, clq.inters)
}

// AllX is like All, but panics if an error occurs.
func (clq *CampaignLinkQuery) AllX(ctx context.Context) []*CampaignLink {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CampaignLink IDs.
func (clq *CampaignLinkQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if clq.ctx.Unique == nil && clq.path != nil {
		clq.Unique(true)
	}
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryIDs)
	if err = clq.Select(campaignlink.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CampaignLinkQuery) IDsX(ctx context.Context) []int64 {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CampaignLinkQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryCount)
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, clq, querierCount[*CampaignLinkQuery](), clq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CampaignLinkQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CampaignLinkQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryExist)
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CampaignLinkQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CampaignLinkQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CampaignLinkQuery) Clone() *CampaignLinkQuery {
	if clq == nil {
		return nil
	}
	return &CampaignLinkQuery{
		config:       clq.config,
		ctx:          clq.ctx.Clone(),
		order:        append([]campaignlink.OrderOption{}, clq.order...),
		inters:       append([]Interceptor{}, clq.inters...),
		predicates:   append([]predicate.CampaignLink{}, clq.predicates...),
		withCampaign: clq.withCampaign.Clone(),
		// clone intermediate query.
		sql:  clq.sql.Clone(),
		path: clq.path,
	}
}

// WithCampaign tells the query-builder to eager-load the nodes that are connected to
// the "campaign" edge. The optional arguments are used to configure the query builder of the edge.
func (clq *CampaignLinkQuery) WithCampaign(opts ...func(*CampaignQuery)) *CampaignLinkQuery {
	query := (&CampaignClient{config: clq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	clq.withCampaign = query
	return clq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UniqueCode string `json:"unique_code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CampaignLink.Query().
//		GroupBy(campaignlink.FieldUniqueCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CampaignLinkQuery) GroupBy(field string, fields ...string) *CampaignLinkGroupBy {
	clq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CampaignLinkGroupBy{build: clq}
	grbuild.flds = &clq.ctx.Fields
	grbuild.label = campaignlink.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UniqueCode string `json:"unique_code,omitempty"`
//	}
//
//	client.CampaignLink.Query().
//		Select(campaignlink.FieldUniqueCode).
//		Scan(ctx, &v)
func (clq *CampaignLinkQuery) Select(fields ...string) *CampaignLinkSelect {
	clq.ctx.Fields = append(clq.ctx.Fields, fields...)
	sbuild := &CampaignLinkSelect{CampaignLinkQuery: clq}
	sbuild.label = campaignlink.Label
	sbuild.flds, sbuild.scan = &clq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CampaignLinkSelect configured with the given aggregations.
func (clq *CampaignLinkQuery) Aggregate(fns ...AggregateFunc) *CampaignLinkSelect {
	return clq.Select().Aggregate(fns...)
}

func (clq *CampaignLinkQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range clq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, clq); err != nil {
				return err
			}
		}
	}
	for _, f := range clq.ctx.Fields {
		if !campaignlink.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CampaignLinkQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CampaignLink, error) {
	var (
		nodes       = []*CampaignLink{}
		withFKs     = clq.withFKs
		_spec       = clq.querySpec()
		loadedTypes = [1]bool{
			clq.withCampaign != nil,
		}
	)
	if clq.withCampaign != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, campaignlink.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CampaignLink).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CampaignLink{config: clq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := clq.withCampaign; query != nil {
		if err := clq.loadCampaign(ctx, query, nodes, nil,
			func(n *CampaignLink, e *Campaign) { n.Edges.Campaign = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (clq *CampaignLinkQuery) loadCampaign(ctx context.Context, query *CampaignQuery, nodes []*CampaignLink, init func(*CampaignLink), assign func(*CampaignLink, *Campaign)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*CampaignLink)
	for i := range nodes {
		if nodes[i].campaign_links == nil {
			continue
		}
		fk := *nodes[i].campaign_links
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(campaign.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_links" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (clq *CampaignLinkQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	_spec.Node.Columns = clq.ctx.Fields
	if len(clq.ctx.Fields) > 0 {
		_spec.Unique = clq.ctx.Unique != nil && *clq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CampaignLinkQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(campaignlink.Table, campaignlink.Columns, sqlgraph.NewFieldSpec(campaignlink.FieldID, field.TypeInt64))
	_spec.From = clq.sql
	if unique := clq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if clq.path != nil {
		_spec.Unique = true
	}
	if fields := clq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, campaignlink.FieldID)
		for i := range fields {
			if fields[i] != campaignlink.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CampaignLinkQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(campaignlink.Table)
	columns := clq.ctx.Fields
	if len(columns) == 0 {
		columns = campaignlink.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.ctx.Unique != nil && *clq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CampaignLinkGroupBy is the group-by builder for CampaignLink entities.
type CampaignLinkGroupBy struct {
	selector
	build *CampaignLinkQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CampaignLinkGroupBy) Aggregate(fns ...AggregateFunc) *CampaignLinkGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the selector query and scans the result into the given value.
func (clgb *CampaignLinkGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, clgb.build.ctx, ent.OpQueryGroupBy)
	if err := clgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CampaignLinkQuery, *CampaignLinkGroupBy](ctx, clgb.build, clgb, clgb.build.inters, v)
}

func (clgb *CampaignLinkGroupBy) sqlScan(ctx context.Context, root *CampaignLinkQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*clgb.flds)+len(clgb.fns))
		for _, f := range *clgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*clgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CampaignLinkSelect is the builder for selecting fields of CampaignLink entities.
type CampaignLinkSelect struct {
	*CampaignLinkQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cls *CampaignLinkSelect) Aggregate(fns ...AggregateFunc) *CampaignLinkSelect {
	cls.fns = append(cls.fns, fns...)
	return cls
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CampaignLinkSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cls.ctx, ent.OpQuerySelect)
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CampaignLinkQuery, *CampaignLinkSelect](ctx, cls.CampaignLinkQuery, cls, cls.inters, v)
}

func (cls *CampaignLinkSelect) sqlScan(ctx context.Context, root *CampaignLinkQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cls.fns))
	for _, fn := range cls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
